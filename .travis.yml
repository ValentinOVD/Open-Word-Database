# Travis CI

language: c

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get install -qq gettext

before_script:
  - env | sort
  - uname -a
  - chmod +x sweetbuild.sh
  - chmod +x sweetupload.sh

script:
  - "./sweetbuild.sh"
  
after_success:
  - "./sweetupload.sh"
